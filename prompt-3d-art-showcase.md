<task>
สร้างเว็บไซต์ single-page สำหรับนำเสนอผลงานศิลปะชิ้นเดียว ในรูปแบบเดียวกับหน้าเปิดตัวสินค้า (product launch page) ของแบรนด์ระดับ Apple / Bang & Olufsen: โมเดล 3D เป็นตัวเอกกลางจอ กล้องเคลื่อนตาม scroll แบบมีสคริปต์ตายตัว และมีเส้น callout ชี้ไปยังชิ้นส่วนต่าง ๆ พร้อมคำอธิบาย

นี่คือโปรเจกต์ส่งอาจารย์ และจะ deploy ขึ้น Vercel ทำให้เสร็จครบทั้งฟีเจอร์ในรอบเดียว ไม่ต้องทิ้ง stub, placeholder หรือ TODO ในโค้ด
</task>

<inputs>
- `./INFO.md` — เนื้อหาทั้งหมดของผลงาน (ประวัติ, แนวคิด, วัสดุ, วิศวกรรม) **อ่านไฟล์นี้ก่อนเขียนโค้ดบรรทัดแรกเสมอ**
- `./photo/` — ภาพถ่ายผลงานจริง ใช้เป็น reference ในการปั้นโมเดล 3D และเป็นภาพประกอบในเว็บ ให้เปิดดูทุกไฟล์ก่อนเริ่มออกแบบ scene
</inputs>

<artwork_context>
<!-- เติมส่วนนี้เอง ถ้าไม่แน่ใจ ให้ดึงจาก INFO.md แทน -->
- ชื่อผลงาน: [...]
- ประเภท/สื่อ: [...]
- ชิ้นส่วนหลักที่ต้องมี callout: [เช่น ตีนตะขาบ, เกราะหน้า, เกราะหลัง, ข้อต่อ, ฐาน]
- โทนที่ต้องการ: [เช่น industrial / brutalist / retro-futurist]
</artwork_context>

<tech_stack>
- Next.js (App Router) + TypeScript + Tailwind CSS
- React Three Fiber + drei สำหรับ 3D
- GSAP ScrollTrigger (หรือ Lenis + scroll progress ของตัวเอง) สำหรับ scroll-driven animation
- ต้อง `npm run build` ผ่านสะอาด และ deploy Vercel ได้ทันทีโดยไม่ต้องตั้งค่าเพิ่ม
- ไม่ใช้ dependency ที่ไม่จำเป็น ไม่ใช้ library ที่ต้องมี API key หรือ backend
</tech_stack>

<experience_spec>
โครงสร้างหน้าเว็บเป็น scroll-driven sequence ที่ scene 3D ตรึงอยู่เต็มจอ (`position: sticky`) ตลอด แล้วเนื้อหาเลื่อนทับ:

1. **Hero** — โมเดลลอยกลางจอ หมุนช้า ๆ ชื่อผลงาน + บรรทัดเดียวว่าคืออะไร
2. **Overview** — กล้องถอยออก เห็นทั้งชิ้น พร้อม callout 3–4 จุดบอกภาพรวมโครงสร้าง
3. **Part sections (หนึ่ง section ต่อชิ้นส่วน)** — กล้องเคลื่อนไปโฟกัสชิ้นส่วนนั้น ส่วนอื่นของโมเดลหรี่ลง (dim/desaturate) เส้น callout ลากจากจุดบนโมเดลไปยังกล่องข้อความ แต่ละ section ต้องตอบ 4 ข้อนี้จาก INFO.md:
   - ชิ้นส่วนนี้ทำหน้าที่อะไร
   - สร้างขึ้นเพื่อจุดประสงค์ใด
   - แรงบันดาลใจ / ที่มาเชิงประวัติศาสตร์
   - วิศวกรรมหรือเทคนิคเบื้องหลัง (วัสดุ, วิธีขึ้นรูป, ข้อจำกัดที่ต้องแก้)
4. **Story / Context** — ประวัติความเป็นมาและแนวคิดของผลงานทั้งชิ้น เป็น timeline หรือ long-form พร้อมภาพจาก `./photo/`
5. **Specs** — ตารางวัสดุ/ขนาด/เทคนิค
6. **Closing** — ชื่อสมาชิก และ (ถ้าทำได้) ปลดล็อกให้ผู้ใช้ orbit โมเดลได้อิสระเป็นครั้งแรก

กฎการเคลื่อนกล้อง: กล้องถูกควบคุมด้วยค่า scroll progress อย่างเดียว (deterministic) ห้ามมี free orbit ระหว่าง sequence, เลื่อนขึ้น-ลงแล้วต้องกลับมาเฟรมเดิมเป๊ะ, ใช้ easing แบบ damped ไม่ใช่ linear
</experience_spec>

<callout_system>
- ทำเป็นระบบ ไม่ใช่ hardcode ทีละอัน: กำหนด anchor เป็นพิกัด 3D ของชิ้นส่วน แล้ว project มาเป็นพิกัดจอทุกเฟรม
- เส้นลากเป็น SVG overlay (leader line + จุดวงกลมที่ปลาย) วาดทับ canvas วาง label ให้ไม่ทับตัวโมเดล
- ข้อมูล callout ทั้งหมดอยู่ในไฟล์ data เดียว (`content/callouts.ts`) แก้ข้อความได้โดยไม่ต้องแตะโค้ด scene
- เส้นและ label fade in/out ตาม section ที่ active
</callout_system>

<model_spec>
- ปั้นโมเดลด้วย R3F primitives + geometry ที่เขียนเอง โดยอ้างอิงสัดส่วนจากภาพใน `./photo/` ไม่ต้องเหมือน 100% แต่ต้องอ่านออกว่าเป็นชิ้นไหนและสัดส่วนต้องใกล้เคียงของจริง
- ตั้งชื่อ mesh/group ทุกชิ้นส่วนให้ตรงกับ key ที่ callout ใช้อ้างอิง
- แยกโค้ดโมเดลไว้ในไฟล์เดียว (`components/ArtworkModel.tsx`) และเขียนให้ **สลับไปโหลดไฟล์ GLTF ได้โดยแก้จุดเดียว** เผื่อภายหลังมีโมเดลสำเร็จรูปมาแทน
- แสง: three-point lighting + environment map ให้เห็น material ชัด (metalness/roughness ตามวัสดุจริงที่ระบุใน INFO.md)
</model_spec>

<content_rules>
- เนื้อหาทุกบรรทัดต้องมาจาก `INFO.md` เท่านั้น **ห้ามแต่งข้อมูลประวัติศาสตร์ วัสดุ หรือรายละเอียดทางเทคนิคขึ้นเอง**
- ถ้า INFO.md ไม่มีข้อมูลที่ section หนึ่งต้องใช้ ให้ใส่ข้อความ `[ต้องการข้อมูลเพิ่ม: ...]` ให้เห็นชัดในหน้าเว็บ แล้วสรุปรายการที่ขาดทั้งหมดให้ฉันตอนจบ
- ภาษาของเนื้อหาบนเว็บ: ตามภาษาที่ใช้ใน INFO.md
</content_rules>

<quality_bar>
- 60fps บนโน้ตบุ๊กระดับกลาง — จำกัด draw call, ไม่ใช้เงาความละเอียดสูงเกินจำเป็น
- Responsive จริงถึงความกว้าง 360px: บนมือถือลดจำนวน callout ที่แสดงพร้อมกัน และย้าย label มาไว้ใต้ scene ถ้าจอแคบเกิน
- รองรับ `prefers-reduced-motion`: ปิด scroll-jack ให้เหลือเป็นหน้า static ที่อ่านครบทุกเนื้อหา
- มี loading state ระหว่างโหลด scene ไม่ปล่อยจอขาว
- Alt text บนภาพทุกใบ, heading hierarchy ถูกต้อง, contrast ผ่าน WCAG AA
- ไม่มี error หรือ warning ใน console
</quality_bar>

<definition_of_done>
เสร็จเมื่อ: `npm run build` ผ่าน, เว็บรันได้ครบทุก section ตาม experience_spec, callout ชี้ตรงจุดทุกความกว้างจอ, เนื้อหาถูกดึงจาก INFO.md จริง, และมี README สั้น ๆ บอกวิธีรัน/deploy กับวิธีสลับโมเดลเป็นไฟล์ GLTF
</definition_of_done>

<scope>
ส่งมอบตามขอบเขตนี้ ตัดสินใจเรื่องรายละเอียดงานออกแบบเองได้เลยโดยไม่ต้องถาม ถามกลับเฉพาะกรณีที่ตีความได้หลายแบบแล้วผลลัพธ์ต่างกันมาก ถ้าเห็นว่ามีวิธีที่ดีกว่าที่ฉันสั่ง ให้บอกสั้น ๆ หนึ่งประโยคแล้วทำตามที่สั่งไปก่อน ทำให้จบทั้งงาน และหยุดตรงขอบเขตนี้ ไม่ต้องเพิ่ม feature ที่ไม่ได้ขอ เช่น CMS, ระบบหลายภาษา, หรือหน้า blog

ใช้ subagent เฉพาะงานใหญ่ที่แยกกันได้จริง งานที่จบได้ในไม่กี่ tool call ให้ทำเองเลย
</scope>

<communication>
ก่อน tool call แรก บอกหนึ่งประโยคว่ากำลังจะทำอะไร ระหว่างทางรายงานเฉพาะตอนเจอเรื่องสำคัญหรือเปลี่ยนทิศทาง ตอนจบให้ขึ้นต้นด้วยผลลัพธ์ก่อนรายละเอียด และเขียน README ให้พอดีเนื้อหา ไม่ต้องมี section เติมน้ำ
</communication>
